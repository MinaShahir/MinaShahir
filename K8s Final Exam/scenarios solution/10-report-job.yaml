apiVersion: batch/v1
kind: Job
metadata:
  name: report-generator
spec:
  template:
    spec:
      containers:
      - name: reporter
        image: bash
        command: ["sh", "-c", "echo \"Report generation done\""]
      restartPolicy: Never
  backoffLimit: 1
