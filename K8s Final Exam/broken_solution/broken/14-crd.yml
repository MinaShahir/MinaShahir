apiVersion: apiextensions.k8s.io/v1
kind:   
metadata:
  name: widgets.mydomain.com
spec:           # CHANGED: 'specification' was incorrect key; should be 'spec' for CRDs
  group: mydomain.com
  versions:
    - name: v1
      served: true
      storage: true
      schema: 
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
  scope: Namespaced
  names:
    plural: widgets
    singular: widget
    kind: Widget
