apiVersion: batch/v1
kind: CronJob
metadata:
  name: cleanup
spec:
  schedule: "*/10 * * * *" #FIXED: 'schdule' to 'schedule'
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: job
              image: busybox
              command: ["sh","-c","echo running"]
          restartPolicy: OnFailure
