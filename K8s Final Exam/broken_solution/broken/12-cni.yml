apiVersion: networking.k8s.io/v1
kind: NetworkPolicy   # FIXED: corrected misspelled 'NetwrokPolicy' -> 'NetworkPolicy'
metadata:
  name: deny-all
spec:
  podSelector: {}
  policyTypes:
    - Ingress
